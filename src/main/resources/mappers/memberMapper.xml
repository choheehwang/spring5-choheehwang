<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- 
memberMapper.xml 역할: 회원 관리에 대한 모든 쿼리를
Mybatis session factory class에서 모아 관리 
 -->
<mapper namespace="memberMapper">

<select id="selectMember" resultType="org.edu.vo.MemberVO">
select * from tbl_member
</select>

<insert id="insertMember">
INSERT INTO tbl_member(
	user_id,
	user_pw,
	user_name,
	email,
	point,
	enabled,
	levels,
	reg_date
	)
VALUES (
	#{user_id},
	#{user_pw},
	#{user_name},
	#{email},
	#{point},
	#{enabled},
	#{levels},
	now()
	<!-- now는 현재 시간 나오게 한다 -->
)
</insert>
<!-- jsp에서 변수표현 EL표기법 ${변수명}, mybatis에서 변수표현 표기법 #{변수명} -->
<!-- mybatis 장점: query 안 쪽에 조건을 제시할 수 있음 -->
<update id="updateMember">
UPDATE tbl_member
SET 
    user_name = #{user_name},
    user_pw = #{user_pw},
    email = #{email},
    point = #{point},
    enabled = #{enabled},
    levels = #{levels},
    update_date = now()
WHERE
    user_id = #{user_id}
</update>

<delete id="deleteMember">
DELETE FROM tbl_member WHERE user_id=#{user_id}
</delete>

<select id="readMember" resultType="org.edu.vo.MemberVO">
SELECT * FROM tbl_member WHERE user_id=#{user_id}
</select>

</mapper>